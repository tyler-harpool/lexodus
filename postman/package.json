{
  "name": "lexodus-contract-tests",
  "version": "2.0.0",
  "description": "Role-based API contract tests for Lexodus",
  "main": "generate-collection.js",
  "scripts": {
    "generate": "node generate-collection.js",
    "test:contract": "node generate-collection.js && npx newman run lexodus-contract.json -e environments/local.json",
    "test:role": "npx newman run lexodus-contract.json -e environments/local.json --folder"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "newman": "^6.2.1"
  }
}
